<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.call4paperz.android"
          android:versionCode="2"
          android:versionName="2.0">

    <uses-sdk
            android:minSdkVersion="7"
            android:targetSdkVersion="17" />

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>

    <application android:label="@string/app_name"
                 android:name=".Call4PaperzApplication"
                 android:icon="@drawable/ic_launcher"
                 android:theme="@style/Theme.Sherlock.Light"
            >
        <activity android:name=".activities.EventsActivity" android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".activities.EventActivity" android:label="@string/app_name"/>
        <activity android:name=".activities.ProposalsActivity" android:label="@string/app_name"/>
    </application>

</manifest>
